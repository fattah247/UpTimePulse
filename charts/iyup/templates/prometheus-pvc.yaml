apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "iyup.fullname" . }}-prometheus-data
  labels:
    {{- include "iyup.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.prometheus.pvcSize }}

{{- if .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "iyup.fullname" . }}-ingress
  labels:
    {{- include "iyup.labels" . | nindent 4 }}
spec:
  rules:
    - host: {{ .Values.ingress.host | quote }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "iyup.fullname" . }}-api-gateway
                port:
                  number: {{ .Values.service.apiGatewayPort }}
{{- end }}

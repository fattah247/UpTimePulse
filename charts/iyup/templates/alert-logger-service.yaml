apiVersion: v1
kind: Service
metadata:
  name: {{ include "iyup.fullname" . }}-alert-logger
  labels:
    {{- include "iyup.labels" . | nindent 4 }}
    app.kubernetes.io/component: alert-logger
spec:
  selector:
    {{- include "iyup.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: alert-logger
  ports:
    - name: http
      port: {{ .Values.service.alertLoggerPort }}
      targetPort: 8080

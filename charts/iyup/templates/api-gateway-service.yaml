apiVersion: v1
kind: Service
metadata:
  name: {{ include "iyup.fullname" . }}-api-gateway
  labels:
    {{- include "iyup.labels" . | nindent 4 }}
    app.kubernetes.io/component: api-gateway
spec:
  selector:
    {{- include "iyup.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: api-gateway
  ports:
    - name: http
      port: {{ .Values.service.apiGatewayPort }}
      targetPort: 8080

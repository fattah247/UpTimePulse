# Placeholder ingress
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: uptimpulse-ingress
spec:
  rules:
    - host: uptimpulse.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: api-gateway
                port:
                  number: 80
